// wallet.routes.js
// This route file is no longer used - kept as an empty router in case other code references it
const express = require('express');
const router = express.Router();

// All wallet-related functionality has been moved to the frontend directly via MetaMask
// No API routes remain

module.exports = router;